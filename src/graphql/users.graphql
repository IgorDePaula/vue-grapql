query Users($page:Int!){
                  users(count:2, page:$page){
                    paginatorInfo{
                      total
                    }
                    data{
                      id,
                      name
                    }
                  }
                }
